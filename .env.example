# Токен НОВОГО бота (создать у @BotFather) — он открывает Mini App и шлёт уведомления.
BOT_TOKEN=your_new_bot_token

# Токен kinopoisk.dev — выдаёт @kinopoiskdev_bot в Telegram (без email!).
# Несколько токенов через запятую — ротация в kinopoisk.py суммирует их лимиты.
KINOPOISK_TOKEN=your_kinopoisk_token

# Дневной бюджет реальных HTTP-вызовов Kinopoisk. Если не задан — auto-обчислюється:
# KP_DAILY_PER_TOKEN (за умовч. 180) × кількість токенів вище. 4 токени → 720/добу.
# Задай явно, якщо хочеш інший ліміт (напр. нижчий для free-тарифу).
# DAILY_SEARCH_BUDGET=
# KP_DAILY_PER_TOKEN=180

# Local-first catalog: known films never expire; positive query results stay six
# months, while empty queries are cached briefly so typos do not burn the quota.
# SEARCH_CACHE_TTL_SEC=15552000
# SEARCH_EMPTY_CACHE_TTL_SEC=21600

# OMDb — fallback-источник (ключ можно взять тот же, что в movie_bot).
OMDB_API_KEY=your_omdb_api_key

# PostgreSQL в production (Neon). Локально оставь пустым — будет SQLite.
DATABASE_URL=

# Встроенная админка подборок. Эти Telegram ID всегда имеют доступ к созданию
# и редактированию подборок; обычные пользователи видят их только для чтения.
ADMIN_USER_IDS=123456789,987654321

# Служебный токен для закрытых endpoint-ов обслуживания (не обязателен локально).
ADMIN_TOKEN=

# Sentry errors + a small, non-PII performance sample. Set 0 to disable traces.
# SENTRY_DSN=
# SENTRY_TRACES_SAMPLE_RATE=0.05

# Bounded in-process performance and data caches.
# REQUEST_METRICS_MAX_SAMPLES=500
# STATS_CACHE_TTL_SEC=90
# STATS_CACHE_MAX_ENTRIES=2000
# GENRES_CACHE_TTL_SEC=300
# RATE_LIMIT_MAX_TRACKED_KEYS=20000

# Image proxy hardening. Cache misses are limited per client and globally queued;
# the disk cache evicts least-recently-used files after either ceiling.
# IMAGE_PROXY_MAX=120
# IMAGE_PROXY_WINDOW=60
# IMG_FETCH_CONCURRENCY=8
# IMG_FETCH_WAIT_SECONDS=1
# IMG_CACHE_MAX_BYTES=268435456
# IMG_CACHE_MAX_FILES=4000
# IMG_CACHE_TRIM_INTERVAL_SECONDS=60

# Partner-avatar links are scoped to the active pair and expire quickly.
# AVATAR_URL_TTL_SECONDS=300
