"""Версионированные движки подбора: V1 (продакшен) и V2 (обкатка).

Правило одно: переменная окружения решает версию ТОЛЬКО в момент создания
сессии. Все последующие запросы диспетчеризуются по версии, записанной в самой
сессии, — иначе выключение флага перевело бы начатую сессию на другой движок
прямо посреди опроса.
"""
